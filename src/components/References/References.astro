---
import { refImages } from "../../data/data";
---

<section id="ref">
   <div class="flex flex-col items-center justify-between max-w-6xl px-10 mx-auto space-y-16 my-44 md:flex-row md:space-y-0">
      {refImages.map((item) => <img src={item.img} alt={item.alt} class="md:w-28 lg:w-44 "/>)}
   </div>
</section>
